apiVersion: tekton.dev/v1alpha1
kind: PipelineResource
metadata:
  name: my-tekton-example
spec:
  type: git
  params:
    - name: url
      value: https://github.com/torinouq/tekton-example
    - name: revision
      value: master
